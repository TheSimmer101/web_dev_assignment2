# React + Vite
A button centered on the page via flexbox. 
When the button is clicked, the background color of the site changes.
The possible colors are stored in an array, and the button cycles through those colors.
Array index updated through useState.

Text displays the current color as well as the next color.
